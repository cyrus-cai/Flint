import { MdxLayout } from "components/mdx-layout.tsx";

export const meta = {
  slug: "v0.9.1-release",
  publishedAt: "2026-01-17T18:00:00.000Z",
  title: "HyperNote v0.9.1",
  headerImage: "/images/changelog/v0.9.1-header.png",
  authors: [
    {
      name: "HyperNote Team",
      description: "Product Team",
      avatarUrl: "https://hypernote.app/favicon.ico",
    },
  ],
};

## Features

- Subscription status now clearly shown in Settings
- Destructive actions (like delete) now appear in red
- Better Chinese localization




export default ({ children, ...rest }) => (
  <MdxLayout meta={meta} {...rest}>
    {children}
  </MdxLayout>
);
